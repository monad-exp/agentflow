[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "agentflow"
version = "0.1.0"
description = "Parallel and DAG orchestration for Codex, Claude, Kimi, and Pi agents"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "OpenAI Codex CLI" }]
dependencies = [
  "boto3>=1.35.0",
  "fastapi>=0.116.0",
  "httpx>=0.28.1",
  "jinja2>=3.1.6",
  "jsonschema>=4.25.0",
  "pydantic>=2.11.0",
  "PyYAML>=6.0.2",

  "typer>=0.16.0",
  "uvicorn>=0.35.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.4.0",
  "pytest-asyncio>=1.1.0",
]
sky = [
  "skypilot[all]>=0.12.3.post1",
]

[project.scripts]
agentflow = "agentflow.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["agentflow"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
